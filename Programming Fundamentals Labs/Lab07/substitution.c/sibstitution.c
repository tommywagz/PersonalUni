//Thomas wagner
//z5436162

//30/10/2022

#include <stdio.h>

#define LENGTH 26

int main(void) {

    char subs[LENGTH] = {0};
    char difference[LENGTH] = {0};
    char alph[LEGNTH] = {0};
    printf("Enter mapping: ");
    fgets(subs, LENGTH, stdin);

    for (int i = 0; i <LENGTH; i++){
        char start = 'a';
        alph[i] = start + i;
    }

    char letter;
    int runs = 0;
    printf("Enter text: ");
    fgets(input, LENGTH, stdin);

    for (int i = 0; i < LENGTH; i++) {
        
    }


    for (int i = 0; i < LENGTH; i++) {
        if (letter == alph[i]) {

            i = LENGTH;
        }

        letter    //letter of the alph has been identified
    }
    runs++;
    printf("%c", NAME OF VAR);
    

    

    return 0;
}