
#include <stdio.h>

struct node {
    struct node *next;
    int data;
};

int mixed(struct node *head);

int main(void) {

}

int mixed(struct node *head) {

    int num = 0;

    struct node *current = head;
    while (current != NULL) {
        if () {
            
        }
        current = current->next;
    }

    return num;
}